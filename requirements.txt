# Core dependencies for Structural Context Pipeline
# Python >= 3.8 required

# Data manipulation and analysis
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0

# Structural biology file parsing
biopython>=1.79

# Optional but recommended for performance
# numba>=0.54.0  # Uncomment if using JIT compilation for speed

# Development dependencies (optional)
# Install with: pip install -r requirements-dev.txt